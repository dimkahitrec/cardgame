type HistoryCard = {
  id: string
  title: string
  description: string
  year: number
  bc: boolean // before christ. indicates if the inventiona was created before christ
}

export type {
  HistoryCard
}
